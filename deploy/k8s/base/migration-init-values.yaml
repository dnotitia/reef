apiVersion: v1
kind: Secret
metadata:
  name: reef-migration-secret
type: Opaque
stringData:
  # Placeholder only. Replace this Secret out-of-band before deployment.
  REEF_AKB_MIGRATION_SERVICE_KEY: "replace-before-deploy"
