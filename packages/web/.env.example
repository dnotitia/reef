# reef-web environment variables.
#
# Mostly server-only values. Anything that needs to reach the browser must use a
# NEXT_PUBLIC_* prefix; the only one is NEXT_PUBLIC_AKB_WEB_URL below (a public
# address, not a secret). LLM and GitHub App credentials stay
# server-side; the akb session lives in an httpOnly cookie.

# Base URL of the akb backend. In-cluster: http://backend.akb.svc.cluster.local:8000
# Local dev: http://localhost:8000 (or a port-forward into the cluster)
AKB_BACKEND_URL=http://localhost:8000

# Human authentication mode is explicit and must match AKB's schema_version=2
# capability catalog. Use `local` for AKB password/local-session-rs256-v2 or
# `sso` for the Reef companion OIDC BFF. There is no hybrid/fallback mode.
REEF_AUTH_MODE=local

# Companion OIDC settings. Required when REEF_AUTH_MODE=sso. The issuer is the
# public Keycloak realm URL stamped into token `iss`; transport may be the
# private Kubernetes realm URL used for token/JWKS calls.
# REEF_KEYCLOAK_ISSUER=https://identity.example.com/realms/reef
# REEF_KEYCLOAK_TRANSPORT_URL=http://keycloak.identity.svc.cluster.local:8080/realms/reef
# REEF_KEYCLOAK_CLIENT_ID=reef-web
# REEF_AKB_API_AUDIENCE=https://akb.example.com/api
# REEF_SESSION_REDIS_URL=rediss://redis.example.com:6379/0
# Base64/base64url-encoded 32-byte AES-256-GCM key. Keep in Secret storage.
# REEF_SESSION_ENCRYPTION_KEY=
# Change this namespace on auth mode/issuer/client/key cutover; never reuse an
# old SSO namespace after a local/SSO transition.
# REEF_AUTH_SESSION_NAMESPACE=reef-sso-2026-09
# REEF_PUBLIC_ORIGIN=https://reef-acme.example.com

# Explicit SSO-first presentation opt-in. It redirects only when exactly one
# AKB provider is ready; auth errors never loop back into it.
# REEF_SSO_AUTO_REDIRECT=1

# Public base URL of the akb web app, used to open a linked document in a new tab
# from an issue's "Linked documents" section (REEF-083). Unset → the open action
# is hidden and only copy-URI is offered. Not a secret; safe to inline in the
# browser bundle.
# NEXT_PUBLIC_AKB_WEB_URL=https://akb.example.com

# Optional deployment-managed OpenAI-compatible LLM endpoint. Set all three to
# enable AI, or leave all three empty for a valid disabled state. The URL can
# point to OpenRouter or an akb-platform gateway; Reef does not classify it.
REEF_LLM_API_KEY=
REEF_LLM_BASE_URL=
REEF_LLM_MODEL=

# Deployment-managed GitHub App used for monitored-repo listing and read-only
# grounding. Browser users do not paste or store GitHub tokens.
# REEF_GITHUB_APP_ID=
# REEF_GITHUB_APP_INSTALLATION_ID=
# REEF_GITHUB_APP_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----"

# Optional dev/CI fallback when no GitHub App is configured. This is still a
# deployment-managed server secret, not a per-user browser token.
# REEF_GITHUB_PAT=
